// frontend/src/components/FullsizeImageViewer.jsx

// imports
import { useEffect } from "react";
import { useFullsizeImage } from "../../contexts/FullsizeImageContent";

function FullsizeImageViewer() {
    
}

// modal that displays image
// reads from context
// renders when there's an image
// handles closing (click backdrop, ESC key)