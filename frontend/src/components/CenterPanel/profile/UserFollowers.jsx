// frontend/src/components/CenterPanel/profile/UserFollowers.jsx

// props: activeTab, onTabChange
// can handle the active state styling and click handling

function UserFollowers() {
    // code

    return (
        <>
            <div>"followed by" goes here</div>
        </>
    )
}

export default UserFollowers;

/*
FULL TODO:
- should display:
    - "followed by..."
        - if viewing other user's profile
        - icons "followed by" display names
            - is a link
                - displays the users same as followers/following pages
*/